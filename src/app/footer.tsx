import { Box, Divider } from "@mui/material";

export const Footer = () => {
  return (
    <Box>
      <Divider />
      <Box
        padding={2}
        display="flex"
        alignItems="center"
        justifyContent="center"
      >
        Footer
      </Box>
    </Box>
  );
};
